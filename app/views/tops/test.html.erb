<script
	type="text/javascript"
	src="//maps.google.com/maps/api/js?sensor=false&libraries=geometry"
></script>

<div id="map" style="height: 240px;"></div>
<script type="text/javascript">
google.maps.event.addDomListener(window, "load", function() {

	var TOKYO = new google.maps.LatLng(latlng.lat(), latlng.lng()); //←関数の名前を変更
	var OSAKA = new google.maps.LatLng(latlng.lat(), latlng.lng());

	// 距離
	var distance = google.maps.geometry.spherical.computeDistanceBetween(, );//←関数名を入れる
